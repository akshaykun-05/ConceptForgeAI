{
  "name": "conceptforge-lambda",
  "version": "2.0.0",
  "description": "AWS Lambda functions for ConceptForgeAI platform",
  "main": "validate-idea.js",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-ssm": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["aws", "lambda", "ai", "validation", "research"],
  "author": "ConceptForgeAI",
  "license": "MIT"
}